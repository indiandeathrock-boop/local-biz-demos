# gbp-scoring-rules.md

GBP診断ツールの採点基準。`/gbp-diag` コマンドの採点エンジンはこのファイルを正とする。
スコア設計・配点は確定済み。変更する場合はこのファイルを直接編集し、更新履歴に記録すること。

---

## スコア構造

自動診断100点 + 人間診断100点の二本立て。最終スコアは両者の平均。

- 自動診断のみが完了している段階では「自動診断◯◯点／100点（人間診断は未実施）」と表記する。
- 80点満点などの変則満点は使わない（対外説明が破綻するため）。
- 人間診断はツールの対象外。レポートには人間診断の項目一覧を「要確認項目」として掲載するのみで、自動採点には混ぜない。
- 判定不能な項目が出た場合、除外して比例換算せず「判定不能」と明示し、判定できた項目の得点と満点をそのまま表記する。

---

## 自動診断の配点（合計100点）

| 項目 | 配点 | 採点方法 |
|---|---|---|
| クチコミ数（競合相対） | 25 | 競合平均比の比率 `r = 対象クチコミ数 / 競合平均クチコミ数` で段階評価。`r>=1.0`→25点／`r>=0.5`→15点／`r>=0.1`→8点／`r<0.1`→3点 |
| 基本情報の完備 | 20 | 名称・住所・電話・営業時間・URLの5要素×4点（登録済み数×4） |
| 評価（星） | 15 | エリア内相対順位（対象＋競合の評価値順位）で段階評価。上位1/3→15点／中位1/3→10点／下位1/3→5点。※評価4.0未満は順位に関わらず一律5点。**評価値が同率の場合はクチコミ数（userRatingCount）降順でタイブレークする**（クチコミ数が多いほど評価の信頼性が高いとみなす。2026-07-05追加：評価4.9同率が実際に発生し、根拠のない配列順で「1位」を決めていたため） |
| クチコミ内容の質 | 10 | レビュー本文（最大5件）をClaudeが分析。具体性・好意度に加え、`publishTime`/`relativePublishTimeDescription` から直近レビューの有無・頻度（最近性）を根拠に含めて判定 |
| 主カテゴリの適切性 | 6 | `primaryType`/`types` が業種として適切かをClaudeが判定 |
| 追加カテゴリの有無 | 4 | Places APIのtypes/primaryTypeでは確認不能なため常に判定不能。人間診断で確認（F-7対応、2026-07-05変更） |
| 写真の量 | 10 | API取得件数（上限10件、詳細は後述）で判定。20枚以下→3点／50枚以下→7点／50枚超→10点／0枚→0点。上限到達時は判定不能 |
| 商品・サービス・属性の登録 | 10 | 第三者事業者の登録情報はPlaces APIから取得不能なため常に判定不能。人間診断で確認 |

## 人間診断の項目（レポートに「要確認」として掲載するのみ・自動採点対象外）

| 項目 | 配点 |
|---|---|
| 投稿の頻度と質（月4回目安・APSORA構成・誘導ボタン） | 20 |
| クチコミ返信（全件返信・低評価対応の作法） | 20 |
| 写真の質（カバー・ロゴ・多様性・不安解消の構図） | 15 |
| 説明文・商品説明の訴求力（お客様主語・具体性） | 15 |
| アカウント体制（オーナー確認・複数人管理・動画認証対応） | 10 |
| インサイト分析（検索語句・ルート検索の定期確認） | 10 |
| 外部連携（自社HP充実・SNS連携・ローカルSEO） | 10 |

---

## 基準の根拠（2026年時点の知見）

- Googleのローカルアルゴリズムは Prominence（権威）から Popularity（エンゲージメント）重視へ移行している。
  → クチコミ・投稿・返信など「活動量」系の配点が厚い理由。
- Q&A機能は2025年末に廃止済み。Q&A関連の施策は説明文・属性・投稿への転記で代替する。
- 30日以上更新のないプロフィールは鮮度低下による視認性低下が指摘されている。
- 写真100枚以上・月4枚更新が通話数／ルート検索数を大きく伸ばすという BrightLocal 系データがある。
- 動画認証（video verification）の導入が進行中。オーナー確認の重要性が上昇している。

---

## 競合選定ロジック（Q-1回答・2026-07-05改訂）

### 根本原因の究明結果

旧実装はText Search（自由文クエリ、`"{エリア} {primaryType}"`）を使っていた。
同一パラメータ（テキスト・languageCode=ja・regionCode=JP・locationBias半径3km）で
連続実行した実測比較で、**返却順序・件数が実行のたびに変わる**ことを確認した
（「株式会社エイブル 松戸店」が実行によって候補に入ったり消えたりした）。
languageCode/regionCodeの追加自体が原因ではなく、Text Search内部の関連度ランキングが
検索時点の内部シグナルに依存して揺れることが真因。自由文クエリに再現性を求める設計が
そもそも成立しなかった。

### 現行方式

競合発見はNearby Search（構造化フィルタ、自由文を使わない）に一本化した。

- `includedTypes: [対象事業者のカテゴリ]`（カテゴリ決定ロジックは次項参照）
- `locationRestriction`: 対象事業者の座標（Place Details の `location`）を中心とする circle。
  半径は町名スコープの段階（800m→1.5km→3km、次項）で決まる
- `rankPreference: "POPULARITY"`
- `maxResultCount: 20`（候補プール）

### 町名スコープ（2026-07-14追加・検索範囲の適正化）

**発見された不具合（問題A）**: 浮世絵カフェ蔦重（台東区千束4丁目）の競合に、
FUGLEN ASAKUSA・ダンデライオン・カービィカフェ等、浅草・蔵前・上野の観光エリアの
著名店（クチコミ2,000〜4,000件超）が並んだ。半径3km固定では町の地域密着店と
観光地の看板店が同列に比較され、事業者本人にとって「本当に競っている相手」の
データにならない。

**対応**: 対象事業者の住所（`addressComponents`）から町名を抽出し、競合選定を
町名スコープ化した。

- 町名の抽出: `sublocality_level_2`を優先、無ければ`sublocality_level_1`
  （実測: 台東区千束4丁目→level_2=「千束」、松戸市本町→level_2=「本町」。区部・市部とも
  level_2が町名に相当）。どちらも無い場合は従来の半径3km方式にフォールバックし、
  レポートに「町名スコープ不可のため半径3km方式で算出」と明記する。
- 検索半径は800m→1.5km→3kmの段階拡張。拡張判定は「業種フィルタ通過件数が8件未満」
  という件数閾値のみ（実行時刻・過去結果に依存しない決定的な条件）。
- 選定ソート: 町名一致を最優先 → `userRatingCount`降順 → place id昇順（全て決定的）。
- 採用したスコープは`competitorScope`（mode/townName/locality/radiusUsed/sameTownCount）
  として`data.json`に保存され、レポートの母集団注記に反映される。

**指示書からの意図的な逸脱（理由つき）**: 当初指示は「町名で厳密にポストフィルタし、
8件未満なら半径拡張（町名フィルタ維持）」だったが、実測の結果、蔦重の800m圏cafe検索
20件中「千束」の店は1件（seihitsu）のみで、3kmまで拡張しても町名一致だけでは8件が
埋まらないことを確認した。厳密適用では競合1件となり比較が成立しないため、
「町名一致を最優先ソート＋不足分は同半径圏内の近隣同業種で補完」方式とした。
これにより競合は「同じ町＋徒歩圏の地域店」で構成される。

**実測値（蔦重・業種手動指定cafe）**: 旧方式（3km）ではTOHOシネマズ上野・浅草花月堂・
FUGLEN等の観光大箱8社（クチコミ1,885〜3,971件）。新方式では半径800mで確定し、
seihitsu（千束・127件）を筆頭に日本堤・竜泉・浅草北部の地域カフェ8社
（クチコミ63〜1,288件）となり、「千束 カフェ」のGoogle検索結果と大枠一致。
同一条件2回連続実行で完全一致を確認。

### 業種ポストフィルタ（2026-07-14追加・第3のバグ修正）

**発見された不具合（問題B）**: 蔦重の競合にTOHOシネマズ上野（映画館）が混入した。
原因は、Nearby Searchの`includedTypes`が候補の**types配列への包含**でマッチすること。
TOHOシネマズ上野は館内カフェを持つため`types`に`cafe`を含み（実測で確認、
primaryTypeは`movie_theater`）、`includedTypes: ["cafe"]`の検索に正当にヒットしていた。
つまり「検索フィルタは業種を保証しない」。取得後の再検証が欠けていたのが漏れ。

**対応**: `isSamePrimaryCategory()`（packages/gbp-core/places.js）による
ポストフィルタを、町名スコープとは独立に必ず適用する。候補の`primaryType`が
検索カテゴリと同一業態の場合のみ競合に採用する。判定は3段（全て決定的な文字列比較）:

1. 完全一致
2. 類縁グループ表（`RELATED_PRIMARY_TYPE_GROUPS`。現状 cafe/coffee_shop/tea_house の1グループ）
3. suffix規則: `italian_restaurant`⇔`restaurant`等、Googleのtype命名階層に対応

**実測値**: 蔦重のcafe検索で、映画館（movie_theater）のほか、800m圏に混入していた
マクドナルド（fast_food_restaurant）・すき家（japanese_restaurant）・デニーズ
（family_restaurant）・茶道具店（tea_store）も除外されることを確認。
Text Searchフォールバック経路（次項）は候補が業種typeを持たないため対象外
（キーワード自体が業種を表すため）。

### カテゴリ決定ロジック（2026-07-06追加・不具合修正）

**発見された不具合**: 不動産会社（primaryType: `service`）を診断した際、競合候補に
ドン・キホーテ・ビックカメラ・ホテル等の全く無関係な業態が並んだ。原因は
`primaryType`をそのままNearby Searchの`includedTypes`に渡していたが、`service`
（および`point_of_interest`/`establishment`/`store`/`food`）はGoogleの汎用
バケツ型カテゴリで、あらゆる業態のtypes配列に含まれるため、実質「フィルタなし」
と同義になっていたこと。

**対応**: `resolveCompetitorCategory()`（packages/gbp-core/places.js）で以下の優先順位により
カテゴリを決定する。

1. 人間による業種の手動指定（Web版画面1の「業種」選択、任意項目）
2. `primaryType`が汎用カテゴリでなければそれを採用
3. 汎用カテゴリの場合、`types`配列から汎用でない最初の値を採用
4. すべて汎用の場合のみ、やむを得ずprimaryTypeをそのまま使う（この場合は競合の質が保証されない旨をレポートに記載すべき。現状は`categoryResolution.source: "generic-only"`として`data.json`に記録するのみ）

決定したカテゴリと採用理由（`source`）は`categoryResolution`として`data.json`に保存され、
後から検証可能。

**実装上の注意**: Googleの`types`配列に含まれる値のすべてがNearby Search APIの
`includedTypes`として有効とは限らない（例: `general_contractor`は`types`には
現れるがNearby Searchでは"Unsupported types"エラーになる）。そのため
`fetchCompetitors()`は候補を優先順位順に実際に試行し、APIが拒否した候補は
スキップして次を試す。全候補が拒否された場合のみ`includedTypes`なしで検索する
（`source: "no-type-filter"`）。

**実測値（バリュー・アップHD、primaryType: service, types: [consultant, general_contractor, service, ...]）**:
自動フォールバックでは`consultant`が採用され、不動産会社中心だが一部無関係業種
（法律事務所等）が混入した。業種を「不動産」に手動指定すると8社全てが
不動産会社になった。**汎用カテゴリのケースでは、可能な限り業種の手動指定を推奨する。**

### Text Searchフォールバック（2026-07-06追加・第2のバグ修正）

上記の対策後も、`primaryType`が`general_contractor`（建設業）の事業者（芙蓉建設）で
再度不具合が発生した。原因は`general_contractor`自体がGoogleの分類上は存在する値だが、
**Nearby Search APIのincludedTypesとしては受理されない**（"Unsupported types"エラー）
ことだった。この場合、候補リストの全滅→旧実装のフィルタなしNearby Search
にフォールバックし、テラスモール松戸・21世紀の森と広場・駅など全く無関係な
人気スポットが競合として返っていた。

**対応**: Nearby Search非対応と実測で確認済みのカテゴリ（`general_contractor`,
`photographer`）については、日本語キーワードでのText Searchにフォールバックする
（`deriveTextSearchKeyword()`）。Text Searchは自由文検索のため実行毎の非決定性
というQ-1の懸念があるが、これは「Nearby Searchの構造化フィルタが原理的に
使えないカテゴリでの最終手段」に限定しており、通常の競合検索経路（大多数の業種）
には影響しない。芙蓉建設で検証: フィルタなし検索時は無関係スポットのみ、
Text Searchフォールバック（キーワード「建設会社」）適用後は8社全て地域の
建設会社になることを確認。

**Nearby Search非対応が判明しているGoogleカテゴリ一覧（実測・2026-07-06時点）**:
`general_contractor`, `photographer`, `hvac_contractor`, `construction_company`
（`construction_company`は`types`に出現しないため通常影響なし）。
新たに発見した場合は`KNOWN_INVALID_TYPE_KEYWORDS`（packages/gbp-core/places.js）
に追記する。

同一条件で2回連続実行し、候補セットが完全一致することを実測で確認済み。
取得した候補プール（最大20件）から、こちらのコード側で `userRatingCount` 降順に
決定論的にソートし、上位8件を競合として採用する（タイは place id 昇順で確定）。
この最終ソート・選定はGoogle APIの応答に依存しないため完全に再現可能。

### 残る非決定性

Nearby Search自体の候補プール（POPULARITY順の20件）は実測で安定していたが、
Googleの内部データ（人気度シグナル・営業状態等）が実際に更新された場合、
将来的に候補プールの内容が変わる可能性はゼロではない。取得した競合セットは
`data.json` にそのまま保存されるため、後から何と比較したかは常に検証可能。

## 対象事業者の特定精度（2026-07-06追加・2026-07-14改訂）

同名法人が複数存在する場合、Text Searchの1件目が誤った事業者を指す可能性がある。
2026-07-14のエリア入力廃止に伴い、住所はWeb版・Telegram版とも**必須**になった
（検索クエリは「事業者名＋住所」、町名スコープの起点も兼ねる）。
`fetchTargetPlace()`は候補一覧の`formattedAddress`を正規化照合し、
一致するものを優先する。一致がなければ検索結果の1件目を採用する。

## 既知のAPI制約と対応

- Places API (New) の Place Details は `photos` フィールドで写真を返すが、上限件数（実装上10件程度）でキャップされ、GBP上の総枚数は取得できない。
  → 返却件数がキャップに達している場合は「写真の量」を判定不能（上限到達＝活発な可能性、と注記）とし、キャップ未満なら実数として閾値評価する。
- レビューも同様に最大5件までしか取得できない。「クチコミ内容の質」はこの5件のみを根拠に判定し、その旨をレポートに明記する。

## データソースの制約

- スクレイピング（Playwright等でマップページを直接読む）を診断の基盤にしない。Places APIで取得できる範囲で設計する（規約リスク・保守コスト回避のため）。
- Google Places API (New) の Advanced SKU 範囲に収める。1件あたり約$0.02。
- 診断1件（対象＋競合8件）で概算 $0.2 以内、原価目標は1件あたり50円以下。

---

## 更新履歴

- 2026-07-14: 競合選定の町名スコープ化＋業種ポストフィルタ追加（蔦重の診断結果フィードバック対応）。
  - 【事象A】蔦重（台東区千束）の競合が浅草・上野の観光大箱（クチコミ2,000件超）で占められた。根本原因は半径3km固定で観光エリアまで母集団に含めていたこと。対応は町名スコープ化（住所のsublocality_level_2から町名を抽出し、町名一致を最優先・半径800m→1.5km→3kmの段階拡張・不足分は近隣同業種で補完）。実測値: 競合がクチコミ1,885〜3,971件の観光店8社 → 63〜1,288件の地域カフェ8社（seihitsu筆頭）に変化。「千束 カフェ」のGoogle検索結果と大枠一致。
  - 【事象B】カフェ診断の競合にTOHOシネマズ上野（映画館）が混入。根本原因はincludedTypesが候補のtypes配列への包含でマッチする仕様（同館は館内カフェのためtypesにcafeを含む・実測確認）で、取得後のprimaryType再検証が無かったこと。対応は`isSamePrimaryCategory()`ポストフィルタ（完全一致／類縁グループ／suffix規則の3段・全て決定的）。実測値: 映画館・ファストフード・牛丼・ファミレス・茶道具店の混入が全て除外された。
  - 指示書の「町名で厳密フィルタ」からの逸脱: 実測で千束の同業種はNearby候補20件中1件のみと判明したため「町名優先ソート＋近隣補完」に変更（詳細は「町名スコープ」節）。
  - 回帰確認: アルドマーニ・東宝ハウス松戸の機械採点は全項目一致（両者とも競合8社全てが町名「本町」一致となり、3件が同町内の同業へ入れ替わったが採点に影響なし）。蔦重・基準2事業者とも同一条件2回連続実行で競合セット完全一致。
  - 併せてエリア入力を廃止し住所必須に変更（Web版フォーム・Telegram版CLI引数・プレースホルダー架空化）。データ構造に`competitorScope`と`address`を追加、`area`は町名スコープのラベル（例: 台東区千束）を格納する互換フィールドに変更。
- 2026-07-05: 初版作成。
- 2026-07-05: レビューフィードバック反映。
  - カテゴリ設定10点を「主カテゴリの適切性6点（Claude判定可能）」と「追加カテゴリの有無4点（API確認不能・常に判定不能）」に分離（F-7）。理由: 従来は「確認できない」と書きながら加点しており、判定不能原則との整合が取れていなかったため。
  - クチコミ内容の質の判定根拠に `publishTime`/`relativePublishTimeDescription`（最近性）を追加（F-3）。
  - 各項目の点数算出式（閾値）を明記（Q-2）。
  - 競合選定ロジックとその再現性について明記し、位置バイアス（半径3km circle）を追加（Q-1、暫定対応）。
- 2026-07-05: Q-1/Q-2/評価タイブレークの追加修正。
  - 競合選定をText Search（暫定対応）からNearby Search（構造化フィルタ）に全面置き換え。根本原因（Text Searchの関連度ランキングが同一パラメータでも実行毎に揺れる）を実測で特定し、再現性を実測で担保（Q-1）。
  - クチコミ数（競合相対）の得点が同一事業者（アルドマーニ）で3点→8点に変化した事象を確認。原因は競合セットの違い（Text Search時代の非決定性）による競合平均クチコミ数の変動（273.0件→256.8件）で、比率が10%の閾値をまたいだため。Nearby Search化により今後はこの種の変動が競合セット選定の非決定性に起因することはなくなる（Q-2）。
  - 評価（星）の相対順位で同率が実際に発生（東宝ハウス松戸とルームプラスが共に4.9）したことを受け、クチコミ数降順のタイブレークルールを追加。
- 2026-07-06: 競合選定の重大バグ修正＋対象特定精度の向上（Web版フィードバック対応）。
  - 【バグ】不動産会社（primaryType: `service`）の競合候補に無関係な大型店舗（ドン・キホーテ・ビックカメラ・ホテル等）が混入する事象を確認。原因は`service`等のGoogle汎用バケツ型カテゴリをそのままNearby Searchのフィルタに使っていたため、実質フィルタなしになっていたこと。`resolveCompetitorCategory()`を追加し、汎用カテゴリの場合は`types`配列から具体的な分類にフォールバックする方式に修正（詳細は「競合選定ロジック」節）。
  - Web版画面1に「業種」手動指定（任意・25業種）を追加し、自動判定が不適切な場合に上書き可能にした。
  - Web版画面1に「住所」入力（任意）を追加し、同名法人の混同を軽減（「対象事業者の特定精度」節）。
